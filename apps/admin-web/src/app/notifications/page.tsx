'use client';

import { PageShell } from '@/components/page-shell';

export default function NotificationsPage() {
  return (
    <PageShell title="Notifications" subtitle="Push notification management — Phase 4">
      <div className="bg-zinc-900 border border-zinc-800 rounded-2xl p-12 text-center text-zinc-600 text-sm">
        Push notification composer coming in Phase 4.
      </div>
    </PageShell>
  );
}
